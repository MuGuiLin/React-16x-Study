import React from 'react';

const initData = {};

// export const { Provider, Consumer, displayName } = React.createContext(initData);

const Context = React.createContext(initData);

export {Context}

import React from 'react';
import css from './index.less';

const index = () => {
    return (
        <footer className={css.footer}>
            Footer
        </footer>
    );
}

export default index;
